# Deux crates, et la frontière entre eux est vérifiée par le compilateur :
# `kernel` n'a pas `godot` dans ses dépendances, donc il ne peut pas l'appeler.
# C'est la première frontière du projet qui ne repose pas sur la discipline.

[workspace]
resolver = "2"
members = ["kernel", "gdextension"]
